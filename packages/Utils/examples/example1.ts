/* Import modules. */
// @ts-ignore
import { example1 } from '../index.js' // use @evonext/utils in production

;(async (): Promise<void> => {
    console.log('Example1')
    console.log(example1())
})()
